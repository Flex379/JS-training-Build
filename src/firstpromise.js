// setTimeout не вміє повідомляти про завершення — він лише викликає передану функцію. Загорнімо його в проміс, щоб отримати обʼєкт, у якого можна спитати «ти вже готовий?» і повісити на нього then. Ця мініфункція delay буде цеглинкою майже в усіх наступних задачах, тому напишіть її самостійно й запамʼятайте.

import { onNotifyBtnClick } from './js/handlers';
import { refs } from './js/refs';

// Що зробити:
// Написати delay(ms): приймає число мілісекунд, повертає проміс, який нічого не повертає, але переходить у стан «виконано» через заданий час.
// Усередину new Promise передати функцію з одним параметром resolve і викликати setTimeout, передавши resolve як обробник.
// По кліку на кнопку заблокувати її, показати текст очікування і викликати delay(2000).
// У then змінити текст повідомлення і розблокувати кнопку.
// Перевірити: під час очікування сторінка не «зависає» — кнопка просто неактивна, а решта інтерфейсу жива.

// Очікуваний результат: Одразу після кліку зʼявляється текст про очікування, рівно через дві секунди він змінюється на нагадування, кнопка знову активна.

// refs.notifyBtn.addEventListener('click', onNotifyBtnClick);

// ! ---------------------------------------------------------------------

// Головна суперсила then у тому, що значення, яке ви з нього повернули, стає вхідним значенням наступного then. Так будується конвеєр: кожен крок робить одну маленьку операцію над даними й передає результат далі. Розберемо це на перерахунку ціни товару зі знижкою.

// Що зробити:
// Написати getPrice(id), що через 500 мс повертає обʼєкт { id, price: 2400 }.
// Першим then деструктуризувати price з обʼєкта і повернути ціну зі знижкою 20%.
// Другим then округлити отримане число.
// Третім then перетворити число на рядок з гривнями.
// Останнім then записати рядок у DOM.
// Простежити в коді, звідки береться аргумент кожного наступного then.
// Очікуваний результат: Приблизно через пів секунди в блоці зʼявляється «1920 грн». Кожен then отримує рівно те, що повернув попередній.

function getPrice(id) {
  return new Promise(res => {
    setTimeout(() => {
      res({ id, price: 2400 });
    }, 500);
  });
}

getPrice(12)
  .then(({ price }) => {
    return price * 0.8;
  })
  .then(disconuted => {
    return Math.round(disconuted);
  })
  .then(rounded => {
    return `${rounded}` + 'грн';
  })
  .then(price => {
    refs.label.textContent = price;
  });
